import THEMES from "@/constants/theme";

export const DEFAULT_THEME = THEMES.JARVIS;

export const STORAGE_KEY = "ai-workspace-theme";

export const AVAILABLE_THEMES = [
    THEMES.JARVIS,
    THEMES.DARK,
    THEMES.LIGHT,
    THEMES.CYBER,
    THEMES.MATRIX,
    THEMES.SYSTEM,
];