import LANGUAGES from "@/constants/languages";

export const DEFAULT_LANGUAGE = LANGUAGES.ENGLISH;

export const LANGUAGE_STORAGE_KEY = "ai-workspace-language";

export const AVAILABLE_LANGUAGES = Object.values(LANGUAGES);